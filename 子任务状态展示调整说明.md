# 子任务状态展示调整说明

## 背景
根据新的子任务状态判断逻辑，需要调整前后端的状态展示界面，以确保数据展示的一致性和准确性。

## 新的状态分类标准

### 1. 已完成的任务
- **完成**：实际开始和结束时间都在计划时间内（按期完成）
- **延期完成**：实际结束时间超过计划结束时间

### 2. 进行中的任务  
- **进行中**：有实际开始时间但无结束时间，且当前日期在计划时间范围内
- **异常**：有实际开始时间但无结束时间，但当前日期已超过计划结束时间

### 3. 未启动的任务
- **未开始**：当前日期在计划开始时间之前
- **异常**：当前日期在计划时间范围内但无实际开始时间
- **异常**：当前日期超过计划结束时间但无实际开始时间

## 前端调整

### 1. TaskDetailTemplate.vue
- 更新了状态筛选选项，新增"延期完成"和"异常"状态
- 修改了getStatusTagType函数，移除了"已验收"状态，添加了对新状态的支持

### 2. SimplifiedMilestoneTaskDetail.vue
- 更新了状态选项列表，与新的状态标准保持一致

### 3. ProjectStatusSubtasksDetailView.vue  
- 更新了状态筛选下拉选项
- 修改了getStatusTagType函数以支持新的状态分类

### 4. SimplifiedSubTaskDetail.vue
- 更新了状态选项列表

## 后端调整

### 1. common_utils.py
- 重写了determine_task_status_import函数，实现了新的状态判断逻辑
- 确保与前端状态展示保持一致

## 状态颜色映射

| 状态 | Element Plus Tag Type | 颜色含义 |
|------|---------------------|----------|
| 未开始 | warning | 橙色 - 任务尚未启动 |
| 进行中 | info | 蓝色 - 任务正在进行 |
| 完成 | success | 绿色 - 任务按时完成 |
| 延期完成 | danger | 红色 - 任务延期完成 |
| 异常 | danger | 红色 - 任务出现异常 |

## 注意事项

1. **向前兼容**：保留了对旧状态值的处理，确保历史数据仍能正常显示
2. **数据一致性**：前后端状态值和逻辑保持完全一致
3. **用户体验**：通过不同的颜色标签帮助用户快速识别任务状态
4. **扩展性**：新的状态体系为后续功能扩展提供了良好的基础

## 测试建议

1. 验证各状态下任务的正确分类
2. 检查状态筛选功能是否正常工作
3. 确认状态标签颜色显示正确
4. 测试历史数据的兼容性显示