# 问题分类 1 和补充分类 2 功能说明

## 功能概述
在客户反馈管理系统中新增了两个统计图表，用于展示"问题分类 1"和"补充分类 2"的数据分布。

## 数据库变更

### 新增字段
在 `customer_quality` 表中添加了两个新字段:

1. **problem_category_1** (VARCHAR(100))
   - 问题分类 1
   - 用于记录主要的问题分类，如：设计问题/工艺问题等

2. **supplement_category_2** (VARCHAR(100))
   - 补充分类 2
   - 用于记录补充的分类信息，如：原材料/外协加工等

### 迁移脚本
执行以下命令添加字段:
```bash
cd project-backend
python migrations/add_classification_fields.py
```

## 后端 API 变更

### 接口位置
`project-backend/main/routers/feedback_router.py`

### 修改的接口
`GET /api/v1/feedback/problem/stats`

在返回数据中新增了两个字段:
- `category_1_distribution`: 问题分类 1 的分布统计
- `supplement_category_2_distribution`: 补充分类 2 的分布统计

### 返回数据示例
```json
{
  "success": true,
  "data": {
    "total": 100,
    "category_1_distribution": {
      "设计问题": 35,
      "工艺问题": 28,
      "材料问题": 22,
      "操作问题": 15
    },
    "supplement_category_2_distribution": {
      "原材料": 45,
      "外协加工": 30,
      "自主生产": 25
    }
  }
}
```

## 前端变更

### 文件位置
`project-dashboard/src/views/kehu/CustomerFeedback.vue`

### 新增图表
1. **问题分类 1 统计饼图**
   - 位置：第三行左侧
   - 类型：饼图
   - 显示各问题分类 1 的数量和占比

2. **补充分类 2 统计柱状图**
   - 位置：第三行右侧
   - 类型：柱状图
   - 显示各补充分类 2 的数量分布

### 图表样式
- 问题分类 1 饼图：使用标准饼图样式，右侧显示图例
- 补充分类 2 柱状图：使用渐变绿色柱状图，X 轴标签可旋转 30 度避免重叠

## 数据导入变更

### 文件位置
`project-dashboard/src/views/kehu/data.py`

### 列映射
在 Excel 列映射中添加了:
- `"问题分类 1": "problem_category_1"`
- `"补充分类 2": "supplement_category_2"`

### Excel 模板要求
Excel 文件中应包含"问题分类 1"和"补充分类 2"两列，如果不存在这两列，导入时会跳过这两个字段。

## ORM 模型变更

### 文件位置
`project-dashboard/src/views/kehu/orm_model.py`

### 新增字段
```python
problem_category_1 = Column(String(100), comment="问题分类 1（如：设计问题/工艺问题）")
supplement_category_2 = Column(String(100), comment="补充分类 2（如：原材料/外协加工）")
```

## 测试方法

### 运行测试脚本
```bash
python test_classification_api.py
```

测试脚本会:
1. 调用统计 API
2. 检查返回数据中是否包含问题分类 1 和补充分类 2 的统计
3. 显示前 5 条统计数据

### 手动验证
1. 启动后端服务：`cd project-backend && python main/modular_main.py`
2. 启动前端服务：`cd project-dashboard && npm run dev`
3. 访问客户反馈管理页面：`http://localhost:5173/kehu`
4. 查看新增的两个图表是否正常显示

## 注意事项

1. **数据完整性**: 如果数据库中 problem_category_1 或 supplement_category_2 字段为 NULL，这些记录不会计入统计
2. **性能优化**: 统计查询使用了与现有筛选条件相同的 WHERE 子句，支持按问题定性、责任班组、产品类型等筛选
3. **图表渲染**: 当数据为空时，图表不会显示任何内容，建议至少导入一些测试数据
4. **颜色规范**: 遵循项目的图表颜色编码规范，使用渐变色提高视觉效果

## 常见问题

**Q: 图表不显示数据？**
A: 检查以下几点:
- 数据库中是否有 problem_category_1 和 supplement_category_2 的数据
- 后端 API 是否正常返回统计数据
- 浏览器控制台是否有错误信息

**Q: 如何填充这两个字段的数据？**
A: 可以通过以下方式:
- 使用 Excel 导入功能，在 Excel 中添加"问题分类 1"和"补充分类 2"列
- 直接在数据库中更新这两个字段的值
- 在前端编辑界面中添加这两个字段的输入框 (需要额外开发)

**Q: 可以修改图表类型吗？**
A: 可以，修改 CustomerFeedback.vue 中的 renderCategory1PieChart 和 renderSupplement2BarChart 函数中的 ECharts 配置即可。
