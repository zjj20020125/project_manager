# 客户反馈信息管理系统 - 快速开始指南

## 📋 概述

客户反馈信息管理系统是独立于项目管理系统的功能模块，用于管理客户的质量投诉、技术建议、售后服务等反馈信息。

---

## 🚀 快速开始（3 步完成部署）

### 第 1 步：创建数据库表

运行数据库迁移脚本：

```bash
cd D:\desktop\项目管理\project manage\project-backend\migrations
python create_customer_feedback_table.py
```

**或者在 MySQL 中直接执行：**

```sql
CREATE TABLE IF NOT EXISTS `customer_feedback` (
  `feedback_id` INT AUTO_INCREMENT PRIMARY KEY COMMENT '反馈 ID（主键）',
  `customer_name` VARCHAR(100) NOT NULL COMMENT '客户姓名',
  `contact_info` VARCHAR(200) NOT NULL COMMENT '联系方式（电话/邮箱）',
  `project_name` VARCHAR(500) DEFAULT NULL COMMENT '关联项目名称',
  `feedback_type` VARCHAR(50) NOT NULL COMMENT '反馈类型（质量投诉/技术建议/售后服务/其他）',
  `priority` VARCHAR(20) NOT NULL COMMENT '优先级（高/中/低）',
  `title` VARCHAR(500) NOT NULL COMMENT '反馈标题',
  `description` TEXT NOT NULL COMMENT '详细描述',
  `expected_solution` TEXT DEFAULT NULL COMMENT '期望解决方案',
  `status` VARCHAR(20) NOT NULL DEFAULT '待处理' COMMENT '状态（待处理/处理中/已解决/已关闭）',
  `handler` VARCHAR(100) DEFAULT NULL COMMENT '处理人',
  `process_record` TEXT DEFAULT NULL COMMENT '处理记录',
  `solution` TEXT DEFAULT NULL COMMENT '解决方案',
  `create_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `update_time` DATETIME DEFAULT NULL ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  `remarks` TEXT DEFAULT NULL COMMENT '备注',
  INDEX `idx_status` (`status`),
  INDEX `idx_feedback_type` (`feedback_type`),
  INDEX `idx_priority` (`priority`),
  INDEX `idx_create_time` (`create_time`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='客户反馈信息表';
```

---

### 第 2 步：启动后端服务

确保后端服务已经包含客户反馈路由（已完成）：

```bash
# 检查路由是否已注册
# 文件：project-backend/main/modular_main.py
# 确认有以下代码：
from main.routers.feedback_router import router as feedback_router
app.include_router(feedback_router)
```

启动后端服务：

```bash
cd D:\desktop\项目管理\project manage\project-backend
python main/modular_main.py
```

后端服务将在：`http://172.16.33.192:8001`

---

### 第 3 步：访问前端页面

启动前端开发服务器：

```bash
cd D:\desktop\项目管理\project manage\project-dashboard
npm run dev
```

访问地址：`http://localhost:5173/customer-feedback`

---

## 📊 功能特性

### ✅ 核心功能

1. **统计看板**
   - 反馈总数统计
   - 待处理/处理中/已解决数量
   - 类型分布饼图
   - 优先级分布柱状图

2. **反馈列表**
   - 支持分页显示
   - 多条件筛选（状态、类型、优先级、关键词）
   - 实时搜索

3. **反馈管理**
   - 新增客户反馈
   - 查看反馈详情
   - 处理反馈（分配处理人、更新状态、记录解决方案）
   - 删除反馈

4. **数据导出**
   - 支持按条件导出 Excel

---

## 🔧 API 接口说明

### 基础 URL
```
/api/v1/feedback
```

### 接口列表

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /stats | 获取统计数据 |
| GET | /list | 获取反馈列表（分页） |
| GET | /{feedback_id} | 获取反馈详情 |
| POST | / | 创建新反馈 |
| PUT | /{feedback_id} | 更新反馈 |
| DELETE | /{feedback_id} | 删除反馈 |
| GET | /export/excel | 导出数据 |

### 详细 API 文档

查看文件：`project-backend/main/routers/feedback_router.py`

---

## 💾 数据库表结构

### customer_feedback 表

| 字段名 | 类型 | 说明 | 必填 |
|--------|------|------|------|
| feedback_id | INT | 反馈 ID（主键） | 是 |
| customer_name | VARCHAR(100) | 客户姓名 | 是 |
| contact_info | VARCHAR(200) | 联系方式 | 是 |
| project_name | VARCHAR(500) | 关联项目 | 否 |
| feedback_type | VARCHAR(50) | 反馈类型 | 是 |
| priority | VARCHAR(20) | 优先级 | 是 |
| title | VARCHAR(500) | 反馈标题 | 是 |
| description | TEXT | 详细描述 | 是 |
| expected_solution | TEXT | 期望解决方案 | 否 |
| status | VARCHAR(20) | 状态 | 是 |
| handler | VARCHAR(100) | 处理人 | 否 |
| process_record | TEXT | 处理记录 | 否 |
| solution | TEXT | 解决方案 | 否 |
| create_time | DATETIME | 创建时间 | 是 |
| update_time | DATETIME | 更新时间 | 否 |
| remarks | TEXT | 备注 | 否 |

### 枚举值说明

**feedback_type（反馈类型）**
- 质量投诉
- 技术建议
- 售后服务
- 其他

**priority（优先级）**
- 高
- 中
- 低

**status（状态）**
- 待处理
- 处理中
- 已解决
- 已关闭

---

## 🎨 界面截图功能

### 1. 统计卡片区域
- 4 个渐变色卡片展示关键指标
- 点击可跳转（未来扩展）

### 2. 图表分析区域
- 左侧：反馈类型分布（饼图）
- 右侧：优先级分布（柱状图）

### 3. 筛选工具栏
- 关键词搜索框
- 状态下拉选
- 类型下拉选
- 优先级下拉选
- 查询/新增/导出按钮

### 4. 数据表格
- 序号、客户信息、项目、类型、优先级
- 标题、状态、处理人、创建时间
- 操作列：详情、处理按钮

### 5. 对话框
- **新增反馈**：表单录入
- **反馈详情**：完整信息展示
- **处理反馈**：更新状态和解决方案

---

## 🔍 使用示例

### 创建一条新的客户反馈

1. 点击"新增反馈"按钮
2. 填写表单：
   - 客户姓名：张三
   - 联系方式：138****1234
   - 反馈类型：质量投诉
   - 优先级：高
   - 项目名称：DF4B-NG7583 型新能源机车 5#车高压柜
   - 反馈标题：产品表面有划痕
   - 详细描述：收到货后发现产品表面有明显划痕...
   - 期望解决方案：希望重新发货或修复

3. 点击"确定"提交

### 处理客户反馈

1. 在列表中找到待处理的反馈
2. 点击"处理"按钮
3. 填写处理信息：
   - 处理人：李四
   - 更新状态：处理中 → 已解决
   - 处理记录：已与客户沟通，安排返工...
   - 解决方案：已重新发货，单号：SF123456789

4. 点击"提交"保存

---

## 📁 相关文件清单

### 后端文件
```
project-backend/
├── main/
│   ├── routers/
│   │   └── feedback_router.py          # 反馈管理路由（445 行）
│   ├── services/
│   │   └── feedback_service.py         # 反馈管理服务（266 行）
│   └── modular_main.py                 # 主入口（已注册路由）
└── migrations/
    └── create_customer_feedback_table.py  # 数据库迁移脚本
```

### 前端文件
```
project-dashboard/
├── src/
│   ├── api/
│   │   └── index.js                    # 已添加 feedbackApi 接口
│   ├── router/
│   │   └── index.js                    # 已添加路由配置
│   └── views/
│       └── kehu/
│           └── CustomerFeedback.vue    # 客户反馈管理界面（876 行）
```

---

## ⚠️ 注意事项

1. **数据库连接**
   - 确保 MySQL 服务正在运行
   - 检查数据库配置文件：`project-backend/database/database.py`

2. **跨域问题**
   - 后端已配置 CORS 允许所有来源
   - 生产环境请设置具体域名

3. **权限控制**
   - 当前版本无身份验证
   - 生产环境建议添加 JWT 认证

4. **数据验证**
   - 前端有表单验证规则
   - 后端有 Pydantic 模型验证

---

## 🛠️ 故障排查

### 问题 1：后端启动失败

**错误信息**：ModuleNotFoundError: No module named 'feedback_router'

**解决方案**：
```bash
# 检查文件是否存在
dir project-backend\main\routers\feedback_router.py

# 如果不存在，重新创建文件
```

### 问题 2：前端页面空白

**检查步骤**：
1. 打开浏览器控制台（F12）
2. 查看是否有 JavaScript 错误
3. 检查路由是否正确配置
4. 确认组件文件路径正确

### 问题 3：API 请求失败

**检查步骤**：
1. 确认后端服务已启动
2. 检查 API 基础 URL 配置
3. 查看网络请求的响应状态码
4. 检查数据库连接是否正常

---

## 📞 技术支持

如有问题，请联系：
- 开发团队：结构件事业部
- 文档版本：v1.0
- 最后更新：2026-03-20

---

## 🎯 下一步计划

- [ ] 添加用户权限管理
- [ ] 支持附件上传
- [ ] 邮件通知功能
- [ ] 数据统计报表导出
- [ ] 移动端适配

---

**祝您使用愉快！** 🎉
